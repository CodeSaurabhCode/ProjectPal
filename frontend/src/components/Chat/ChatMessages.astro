<div class="chat-messages" id="chatMessages">
	<!-- Messages will be dynamically injected here by chat.ts -->
</div>

<style>
	.chat-messages {
		flex: 1;
		padding: 24px;
		overflow-y: auto;
		display: flex;
		flex-direction: column;
		gap: 24px;
		width: 100%;
		max-width: 960px;
		margin: 0 auto;
	}

	:global(.message) {
		display: flex;
		gap: 16px;
		max-width: 90%;
		align-self: flex-start;
		animation: fadeIn 0.3s ease-out;
		align-items: flex-start;
	}

	:global(.message.user) {
		align-self: flex-end;
		flex-direction: row-reverse;
	}

	@keyframes fadeIn {
		from {
			opacity: 0;
			transform: translateY(10px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	:global(.message-avatar) {
		width: 40px;
		height: 40px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		font-size: 20px;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
	}

	:global(.message.user .message-avatar) {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
	}

	:global(.message.assistant .message-avatar) {
		background: linear-gradient(135deg, #00796b 0%, #004d40 100%);
		color: white;
	}

	:global(.message-content) {
		padding: 16px 20px;
		border-radius: 16px;
		line-height: 1.7;
		background-color: var(--assistant-message-bg);
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
		word-wrap: break-word;
		max-width: 100%;
	}

	:global(.message.user .message-content) {
		background: linear-gradient(135deg, #00796b 0%, #004d40 100%);
		color: white;
		border-bottom-right-radius: 4px;
	}

	:global(.message.assistant .message-content) {
		background-color: var(--card-bg);
		border: 1px solid var(--border-color);
		border-bottom-left-radius: 4px;
	}

	:global(.message-content p) {
		margin: 0 0 12px 0;
	}

	:global(.message-content p:last-child) {
		margin-bottom: 0;
	}

	:global(.message-content h1),
	:global(.message-content h2),
	:global(.message-content h3) {
		margin: 16px 0 12px 0;
		font-weight: 600;
	}

	:global(.message-content ul),
	:global(.message-content ol) {
		margin: 12px 0;
		padding-left: 24px;
	}

	:global(.message-content li) {
		margin: 6px 0;
	}

	:global(.message-content code) {
		background-color: rgba(0, 0, 0, 0.1);
		padding: 2px 6px;
		border-radius: 4px;
		font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
		font-size: 0.9em;
	}

	:global(.message.user .message-content code) {
		background-color: rgba(255, 255, 255, 0.2);
	}

	:global(.message-content pre) {
		background-color: #1e1e1e;
		color: #d4d4d4;
		padding: 16px;
		border-radius: 8px;
		overflow-x: auto;
		margin: 12px 0;
		position: relative;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
	}

	:global(.message-content pre code) {
		background: none;
		padding: 0;
		color: inherit;
		font-size: 0.9em;
	}

	:global(.copy-code-btn) {
		position: absolute;
		top: 8px;
		right: 8px;
		background-color: rgba(255, 255, 255, 0.1);
		color: #d4d4d4;
		border: 1px solid rgba(255, 255, 255, 0.2);
		padding: 6px 12px;
		border-radius: 6px;
		cursor: pointer;
		font-size: 12px;
		opacity: 0;
		transition: all 0.2s;
	}

	:global(.message-content pre:hover .copy-code-btn) {
		opacity: 1;
	}

	:global(.copy-code-btn:hover) {
		background-color: rgba(255, 255, 255, 0.2);
		border-color: rgba(255, 255, 255, 0.3);
	}

	:global(.message-content.empty) {
		display: none;
	}

	:global(.typing-indicator) {
		display: flex;
		align-items: flex-start;
		gap: 16px;
		align-self: flex-start;
		animation: fadeIn 0.3s ease-out;
		padding: 12px 0;
	}

	:global(.typing-indicator .message-avatar) {
		width: 40px;
		height: 40px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(135deg, #00796b 0%, #004d40 100%);
		color: white;
		font-size: 20px;
	}

	:global(.typing-dots) {
		background-color: var(--card-bg);
		padding: 16px 20px;
		border-radius: 16px;
		border-bottom-left-radius: 4px;
		display: flex;
		gap: 6px;
		align-items: center;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
		border: 1px solid var(--border-color);
	}

	:global(.typing-dots span) {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: var(--primary-color);
		animation: typing 1.4s infinite;
		display: inline-block;
	}

	:global(.typing-dots span:nth-child(2)) {
		animation-delay: 0.2s;
	}

	:global(.typing-dots span:nth-child(3)) {
		animation-delay: 0.4s;
	}

	@keyframes typing {
		0%,
		60%,
		100% {
			transform: scale(1);
			opacity: 0.3;
		}
		30% {
			transform: scale(1.2);
			opacity: 1;
		}
	}

	/* Status indicator styles */
	:global(.status-indicator) {
		display: flex;
		gap: 16px;
		align-items: flex-start;
		align-self: flex-start;
		animation: fadeIn 0.3s ease-out;
		padding: 12px 0;
		max-width: 90%;
	}

	:global(.status-indicator .message-avatar) {
		width: 40px;
		height: 40px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(135deg, #00796b 0%, #004d40 100%);
		color: white;
		font-size: 20px;
		flex-shrink: 0;
	}

	:global(.status-content) {
		background-color: var(--card-bg);
		padding: 16px 20px;
		border-radius: 16px;
		border-bottom-left-radius: 4px;
		border: 1px solid var(--border-color);
		border-left: 3px solid var(--primary-color);
		min-width: 280px;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
		flex: 1;
	}

	:global(.status-header) {
		display: flex;
		align-items: center;
		gap: 10px;
		font-size: 15px;
		font-weight: 500;
		color: var(--text-color);
		margin-bottom: 0;
	}

	:global(.status-spinner) {
		display: inline-block;
		width: 16px;
		height: 16px;
		border: 2px solid rgba(0, 121, 107, 0.3);
		border-top-color: var(--primary-color);
		border-radius: 50%;
		animation: spin 0.8s linear infinite;
		flex-shrink: 0;
	}

	@keyframes spin {
		to {
			transform: rotate(360deg);
		}
	}

	:global(.status-text) {
		font-size: 15px;
		opacity: 0.95;
		line-height: 1.6;
		display: flex;
		align-items: center;
		gap: 8px;
	}

	:global(.status-tool) {
		display: inline-flex;
		align-items: center;
		gap: 4px;
		background-color: rgba(0, 121, 107, 0.15);
		padding: 4px 12px;
		border-radius: 6px;
		font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
		font-size: 13px;
		color: var(--primary-color);
		font-weight: 600;
		margin-left: 8px;
		border: 1px solid rgba(0, 121, 107, 0.3);
	}

	@media (max-width: 768px) {
		.chat-messages {
			padding: 16px;
			gap: 16px;
		}

		:global(.message) {
			max-width: 100%;
		}

		:global(.status-content) {
			max-width: 85%;
		}
	}
</style>
